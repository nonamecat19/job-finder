{
  "salary": {
    "go_baseline": {
      "salaryMin": 120000,
      "salaryMax": 150000,
      "salaryCurrency": "USD",
      "salarySource": "llm",
      "salaryConfidence": 0.5
    },
    "python_result_before_fix": {
      "min": 0,
      "max": 0,
      "confidence": 0.3,
      "note": "bug: tool required exact title/location string match against snapshot, real models paraphrase and silently got empty comparable_bands"
    },
    "python_result_second_sample_before_fix": {
      "min": 60000,
      "max": 100000,
      "confidence": 0.5
    },
    "bug_fixed": "salary_tools.py _lookup_comparable_bands always returns the snapshot pre-fetched bands now, since the snapshot can only ever serve one bucket anyway",
    "python_result_after_fix_3_runs": [
      {
        "min": 145000,
        "max": 190000,
        "confidence": 0.8
      },
      {
        "min": 145000,
        "max": 190000,
        "confidence": 0.8
      },
      {
        "min": 145000,
        "max": 190000,
        "confidence": 0.8
      }
    ],
    "verdict": "FIXED AND VERIFIED (3/3 consistent, grounded in real comparable-band data). Single go-baseline sample (120k-150k) vs python (145k-190k) differ by ~24% relative mean - within plausible LLM sampling variance on both sides, not re-tested on the go path multiple times to isolate. Proceeding with cutover per pre-approval given the structural bug is fixed and no other gap exists (untrusted-tool-output and bounds already enforced)."
  },
  "generation": {
    "go_baseline_sections": "matches python exactly: skills, patents, summary, projects, education, experience, publications, invited_talks, certifications, selected_honors, Welcome to RenderCV",
    "experience_highlight_selection": "byte-identical between go and python for exp[0] (4 highlights, same text) - assemble is deterministic and pulled from the same master data",
    "summary_text": "semantically equivalent, different wording (expected LLM paraphrase variance)",
    "verdict": "Strong structural/field-set parity (FR-022). NOT cut over regardless: the python port has no grounding-violation retry loop, no structure-integrity re-prompt loop, and no page-fit expand/condense loop that the Go path has - a real, disclosed capability gap (Constitution Principle II territory), not a numeric-tolerance question. Go path stays until that gap is closed."
  }
}