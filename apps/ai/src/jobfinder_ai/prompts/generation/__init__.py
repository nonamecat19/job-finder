"""Prompts for the `generation` state graph's stages (C6-1), ported verbatim
from `apps/api/internal/generation/application/rendercv_llm.go`. One module
per stage: `analyze`, `select`, `summary`.
"""

from __future__ import annotations
