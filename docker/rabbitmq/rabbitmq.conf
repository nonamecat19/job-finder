# consumer_timeout must exceed the longest work type's MaxDuration with
# margin (contracts/messaging.md M3-6). AI_TASK_TIMEOUT_INGEST is the
# longest at 30m (.env.example); 1h gives headroom without the config
# tracking every timeout value it must exceed.
consumer_timeout = 3600000
